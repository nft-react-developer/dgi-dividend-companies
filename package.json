{
  "name": "dgi-analyzer",
  "private": true,
  "workspaces": [
    "packages/electron",
    "packages/backend",
    "packages/frontend",
    "packages/shared"
  ],
  "scripts": {
    "dev:frontend": "yarn workspace @dgi/frontend dev",
    "dev:backend":  "yarn workspace @dgi/backend dev",
    "dev":        "concurrently \"yarn workspace @dgi/backend dev\" \"yarn workspace @dgi/frontend dev\" \"yarn workspace @dgi/electron dev\"",
    "build":      "yarn workspace @dgi/shared build && yarn workspace @dgi/backend build && yarn workspace @dgi/frontend build && yarn workspace @dgi/electron build",
    "db:migrate": "yarn workspace @dgi/backend migrate",
    "dev:server": "yarn workspace @dgi/backend dev",
    "db:seed":    "yarn workspace @dgi/backend seed",
    "lint":       "yarn workspaces run lint",
    "test":       "yarn workspaces run test"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "typescript":   "^5.4.0"
  }
}